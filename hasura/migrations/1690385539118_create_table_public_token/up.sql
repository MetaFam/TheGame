CREATE TABLE IF NOT EXISTS "public"."token"("address" text NOT NULL, "safe_address" text NOT NULL, "chain_id" integer NOT NULL, "guild_id" uuid NOT NULL, PRIMARY KEY ("address") , FOREIGN KEY ("guild_id") REFERENCES "public"."guild"("id") ON UPDATE no action ON DELETE no action, UNIQUE ("address"), UNIQUE ("safe_address"), UNIQUE ("guild_id"));
