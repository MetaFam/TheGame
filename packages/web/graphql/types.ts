export type Skill = {
  id: string;
  name: string;
  category: string;
};

export type PersonalityType = {
  id: string;
  name: string;
  description: string;
  image: string;
};

export type PlayerType = {
  id: string;
  name: string;
  description: string;
};
