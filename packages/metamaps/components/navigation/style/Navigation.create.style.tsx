import styled from 'styled-components';
import { WhiteColor } from '../../../styles/Styles';

export const NavigationCreateContainer = styled.div`
    width: 100%;
    max-width: 768px;
    margin: auto;
    padding: 15px;

    p {
        font-size: 24px;
        color: ${WhiteColor};
        padding: 0 15px 5px 15px;
    }
`;